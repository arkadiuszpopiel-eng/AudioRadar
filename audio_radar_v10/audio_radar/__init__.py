"""Package init for audio_radar.

This module exposes the package version via the ``__version__``
attribute so that external code and tools can programmatically
inspect the version of the Audio Radar package. When creating
a new release update ``__version__`` here, the ``VERSION``
constant in :mod:`audio_radar.main` and the contents of the
``VERSION.txt`` file accordingly.
"""

# Public version identifier for this release
__version__ = "v10.2"
# Audio Radar Dependencies - Windows Alternative
# Use this if standard requirements.txt fails on Windows
# Python 3.8-3.12 recommended (Python 3.13+ may have compatibility issues)

# Audio capture and processing
sounddevice>=0.4.6
numpy>=1.21.0,<2.0.0
scipy>=1.7.0

# Visualization - Pre-built wheel
# Install pygame separately if this fails:
# pip install pygame==2.5.2 --only-binary :all:
pygame==2.5.2

# Alternative: Install pygame from wheel archive
# Visit: https://www.lfd.uci.edu/~gohlke/pythonlibs/#pygame
# Download appropriate .whl file for your Python version
# Then: pip install pygame‑2.X.X‑cpXX‑cpXX‑win_amd64.whl
